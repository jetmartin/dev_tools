# Status

To knew whats the delta between you workspace and your local Git repository, you can use the status command.
This will show up for each single files the current status. if it's not iso as your Git.

```sh
git status
```
